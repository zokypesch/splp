# Kafka Configuration
KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=my-service
KAFKA_GROUP_ID=my-service-group

# Cassandra Configuration
CASSANDRA_CONTACT_POINTS=localhost
CASSANDRA_LOCAL_DATACENTER=datacenter1
CASSANDRA_KEYSPACE=messaging

# Encryption Key (32 bytes = 64 hex characters)
# Generate using: bun run -e "import {generateEncryptionKey} from './src/index.js'; console.log(generateEncryptionKey())"
ENCRYPTION_KEY=your_64_character_hex_encryption_key_here_must_be_shared_across_services
